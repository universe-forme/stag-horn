import PaymentClient from "@/components/PaymentClient";

export const metadata = {
  title: "Payment",
  description: "Complete your purchase with Wazir Cutlery's",
};

export default function PaymentPage() {
  return <PaymentClient />;
}
